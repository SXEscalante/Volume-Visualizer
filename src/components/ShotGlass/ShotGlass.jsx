import shotGlass from "../../images/full-shot-glass.png"

import "./ShotGlass.css"

const ShotGlass = ({}) => {
    return ( 
        <div>
            <img className="bottle" src={shotGlass} alt="" />
        </div>
    );
}
 
export default ShotGlass;